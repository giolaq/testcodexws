#!/bin/sh
set -eu
repo=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
python="$repo/.factory/venv/bin/python"
if [ ! -x "$python" ]; then python=python3; fi
exec "$python" "$repo/factory/orchestrator.py" "$@"
